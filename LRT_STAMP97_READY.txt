# LRT Stamp 97 READY

**Branch:** stamp-85
**Bump parent:** 53d85fa (versionCode 97 / versionName 1.0.87)
**Tip:** run `git rev-parse HEAD` after follow-up
**Version:** 97 / 1.0.87

## Root cause (one-liner)
drawStageTankMasks mid-split full-stack dest clipped full-frame tank alpha islands (esp. S2 fuel) to ZERO visible fill while fuelRemain drained.

## What fixed
- VehicleDraw.kt alphaBBoxCached / drawStageTankMasks: bandTop/bandBot from mask alpha bbox mapped into dest (not mid-split)
- Hull order: stack/cutaway/side before opaque shell; brighter glassA fills-behind
- CommandConsoleView + Wallpaper + fitHeightForSlot: fleet-wide bigger plates, shorter plumePad
- F9 engShip overlay skipped (MVac baked into ship/upper/s2); flame_* = plume when ENG lit only
- Darren STG2 MVac pack + flame_s2/flame_ship/exhaust_ship polish folded

## F9 fills look on plates (proof)
tank_s1_ox ~32-50% stack H; tank_s1_fuel ~54-76%; tank_s2_ox ~14-19%; tank_s2_fuel ~20-24% — bbox bands map these into dest; mid-split would kill S2 fuel.

## Sync path
D:\Downloads\LiveRocketTracker-vc97-debug.apk

## HOLD
No China/FH. No invented engine_ship. No per-frame getPixel on draw.
